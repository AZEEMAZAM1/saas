const contacts = (state, {type, payLoad})=> {
    switch(type){
        case 'GET_CONTACTS':
            return state;
            


            default:
                return state;
    }
}

export default contacts;